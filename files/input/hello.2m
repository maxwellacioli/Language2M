soma(inteiro a, inteiro b) inteiro {
    inteiro c, c;

    retorne a + c;
};

subtracao(inteiro x, real k, logico z) inteiro {
    inteiro y;

    retorne x - y;
};

principal () inteiro {
    inteiro x, y, w;

    w = soma(x, y);

    imprima(w);
};
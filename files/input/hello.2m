soma (inteiro a) inteiro {
    inteiro r;
    se(a == 0) {
        r = 1;
    } senao {
        r = soma(a - 1);
    };
    retorne r;
}

principal () inteiro {
    inteiro x, y, z;
    x = 10;

    /$z = fib(10);
    z = soma(x);

    imprima(z);
 }
soma(inteiro a, inteiro b) inteiro {
    inteiro c, t;

    retorne a + c;
};

subtracao(inteiro x, inteiro y) inteiro {
    inteiro z;

    retorne x + z - y;
};

principal () inteiro {
    inteiro x, y, w;

    w = soma(x, y);

    imprima(w);
};
soma(inteiro a) inteiro {
    inteiro c, d;

    retorne a + c;
};

subtracao(inteiro x) inteiro {
    inteiro y;

    retorne x - y;
};

principal () inteiro {
    inteiro x, y, w;

    w = soma(x, y);

    imprima(w);
};
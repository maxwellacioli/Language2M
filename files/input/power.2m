principal () inteiro {
    inteiro a, b, temp, cont;

    leia(a);
    leia(b);

    temp = a;

    repita( cont = 1 ; cont < b ; cont = cont + 1 ) {
        temp = temp * a;
    };

    imprima(a ++ "^" ++ b ++ " = " ++ temp);
}